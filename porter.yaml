version: v1stack

build:
  method: pack
  builder: heroku/buildpacks:20
  context: "/Users/ferozemohideen/Desktop/js-test-app"

env:
  API_CLIENT_ID: foobar
  API_CLIENT_SECRET: foobaz

apps:
  web-1:
    run: launcher node index.js
    config:
      resources:
        requests:
          cpu: 200m
          memory: 256Mi
    type: web
  web-2:
    run: launcher node index.js
    config:
      resources:
        requests:
          cpu: 100m
          memory: 512Mi
    type: web

release: yarn release
